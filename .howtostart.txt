start WSL

start Docker
    docker-compose up

    #verify infrastructure using script:
        bash healthcheck.sh 
    #or manually by running the following:
        #Redis
            docker-compose exec redis redis-cli ping
            #should return: PONG
        
        #PostgreSQL
            docker-compose exec postgres qsql -U tft -d tft -c "SELECT 1;"
            #should return: 1
        
        #ClickHouse
            docker-compose exec clickhouse clickhouse-client --query "SELECT 1"
            #should return: 1

        #Flower (web-based monitoring UI for Celery)
            #access http://localhost:555
            #no errors
    




stop Docker
    docker-compose down
    docker-compose down -v #wipes the volumes too